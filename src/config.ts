/**
 * Personalize the experience — edit these before sharing the link.
 */
export const CONFIG = {
  /** Background music in /public/audio/ */
  musicUrl: "/audio/music.mp3",
  /** Optional night ambience — leave empty to skip */
  ambienceUrl: "",
};
